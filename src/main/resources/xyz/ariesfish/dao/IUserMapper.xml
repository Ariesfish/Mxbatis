<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xyz.ariesfish.dao.IUserDao">
    <select id="findAll" resultType="xyz.ariesfish.domain.User">
        select * from user
    </select>

    <insert id="saveUser" parameterType="xyz.ariesfish.domain.User">
        <selectKey keyProperty="id" keyColumn="id" resultType="java.lang.Integer" order="AFTER">
            select last_insert_id()
        </selectKey>
        insert into user (username, address, sex, birthday) values(#{username}, #{address}, #{sex}, #{birthday})
    </insert>

    <update id="updateUser" parameterType="xyz.ariesfish.domain.User">
        update user set username=#{username}, address=#{address}, sex=#{sex}, birthday=#{birthday} where id=#{id}
    </update>

    <delete id="deleteUser" parameterType="java.lang.Integer">
        delete from user where id=#{id}
    </delete>

    <select id="findById" parameterType="java.lang.Integer" resultType="xyz.ariesfish.domain.User">
        select * from user where id=#{id}
    </select>

    <!-- 根据名字模糊查询 -->
    <select id="findByName" parameterType="java.lang.String" resultType="xyz.ariesfish.domain.User">
        select * from user where username like #{username}
    </select>

    <select id="findTotal" resultType="java.lang.Integer">
        select count(id) from user
    </select>

    <select id="findUserByVo" parameterType="xyz.ariesfish.domain.QueryVo" resultType="xyz.ariesfish.domain.User">
        select * from user where username like #{user.username}
    </select>
</mapper>